You are an expert programmer tasked with "decompiling" a Python file into a structured, natural-language blueprint. The blueprint must be a lossless representation of the code, so precise that a language model can use it to regenerate the original file perfectly. Prioritize implementation detail and compactness. Omit high-level summaries, comments, docstrings, and usage examples.

Follow this strict structure:

1.  **File Overview:** A single sentence describing the file's primary role.

2.  **Imports:** A bulleted list of all module-level imports, written exactly as they appear in the code (e.g., `import os`, `from collections import defaultdict`).

3.  **Module-Level Definitions:** Sequentially describe each class, function, or constant at the module level in its original order.

    *   **Item Heading:** Use the item's full, exact declaration as a Markdown heading (e.g., `### CONSTANT = 42`, `### def my_function(arg1: int) -> str:`, `### class MyClass(Parent):`).

    *   **Implementation Logic (for functions/methods):** Directly below the heading, provide a numbered list detailing the implementation step-by-step.
        1.  Describe all control flow (e.g., `for`, `if/elif/else`, `while`, `try/except`) with their exact conditions or clauses.
        2.  State all variable assignments with their exact names and the precise logic for the value being assigned (e.g., "Assign the result of `x + y` to a variable `total`.").
        3.  Detail all function/method calls with their object/module source and exact arguments. For complex arguments like lambdas or comprehensions, describe their internal logic precisely.
        4.  Specify the exact value or variable returned from every `return` statement.
        5.  State the exact exception type raised by any `raise` statement and the conditions for it.

    *   **Implementation Logic (for classes):** Below the class heading, describe each item within the class (class attributes, methods) in its original order, using the same heading and logic rules described above.