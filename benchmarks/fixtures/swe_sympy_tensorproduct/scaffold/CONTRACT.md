# Implementation target

Write the module at `sympy/physics/quantum/tensorproduct.py`.

Other modules import these public names from it, so they MUST exist with these exact names:

- `TensorProduct`
- `matrix_tensor_product`
- `tensor_product_simp`

Implement them to satisfy the specification. Do not write tests.
