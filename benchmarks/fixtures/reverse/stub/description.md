# Reverse fixture (natural-language source stub)

Implement package `reverse` with a function `reverse(s: str) -> str` that returns the string reversed (Python slice semantics). Put code in `src/reverse/__init__.py`. Add pytest tests: `reverse("abc") == "cba"` and `reverse("") == ""`. Configure pytest with `pythonpath = ["src"]` and `testpaths = ["tests"]` in `pyproject.toml`.
